<Page x:Class="OperationCapture.ExecutingMode"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OperationCapture"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ExecutingMode">

    <Page.Resources>
        <ResourceDictionary>
            <BitmapImage x:Key="StartImage" 
                         UriSource="./Resource/再生停止ボタン.png"/>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="1" Click="CaptureStartButton_Click"  ToolTip="Stop Capture">
            <Image Source="{StaticResource StartImage}"/>
        </Button>
    </Grid>
</Page>
